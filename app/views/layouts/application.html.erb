<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Turbo Rails Quote Editor" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <header class="navbar navbar--expand-md navbar--light bg-light mb-4">
    <div class="container navbar__container">
      <%= link_to "Quote Editor", root_path, class: "navbar-brand" %>

      <div class="navbar__auth">
        <% if authenticated? %>
          <span class="nav-user-email"><%= Current.user.email_address %></span>
          <%= button_to "Sign out", session_path, 
                        method: :delete, 
                        class: "btn btn--secondary", 
                        data: { turbo: false } %>
        <% end %>
      </div>
    </div>
  </header>

    <main class="container">
      <%= yield %>
    </main>
  </body>
</html>